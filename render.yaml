services:
  - type: web
    name: monopoly-one
    env: node
    buildCommand: npm install && npm install multer
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: MONGODB_URI
        sync: false
